教学用虚构数据，不是任何真实项目的实测记录。

测量对象：处理固定测试输入 sample_input.csv（1000 行，内容固定）。
环境：同一台机器，Python 3.12，单线程，关闭其他负载。

修改前计时（3 次运行）：8.1 秒、7.9 秒、8.0 秒，取整表述为 8 秒。
修改后计时（3 次运行）：3.0 秒、3.1 秒、2.9 秒，取整表述为 3 秒。

计时方式：从读取输入文件开始，到写出结果文件结束。
